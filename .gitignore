# Dependencies
node_modules/

# Build / Wrangler
dist/
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example

# Secrets — never commit
*.pem
# ...except Bee's PUBLIC CA roots, which are public trust anchors (not secrets)
# and are required in the bridge build context.
!bridge/bee-ca.pem
.env
.env.*

# Logs / OS
*.log
.DS_Store

# Editor
.vscode/
.idea/
