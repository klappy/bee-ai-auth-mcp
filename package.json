{
  "name": "bee-ai-auth-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Self-host-first, OAuth-secured MCP relay exposing a Bee pendant's retrieval API to any MCP client. Phase 1: auth core + whoami. Phase 2: read surface (bee_docs + bee_read). MIT.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:smoke": "vitest run test/smoke.live.test.ts"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7",
    "@cloudflare/workers-oauth-provider": "^0.7.2",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.15.0",
    "tweetnacl": "^1.0.3",
    "uqr": "^0.1.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260610.1",
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8",
    "wrangler": "^4.99.0"
  }
}
