{
  // MERGED INTO ../wrangler.jsonc (root) on feat/bridge-wiring (ledger E0014/E0015).
  // The containers / durable_objects.bindings / migrations block that used to live
  // here now lives in the root project config: a Cloudflare Worker has exactly one
  // wrangler config, and the bridge container binds INTO the relay Worker, reached
  // over an internal Worker->container call (D0028). This file is a tombstone
  // pointer only — do not add config here; edit ../wrangler.jsonc instead.
}
