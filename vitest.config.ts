import { defineConfig } from "vitest/config";

// Minimal for now. When the Phase-2 `docs` tool bundles governance markdown,
// add git-auth's markdown-as-text plugin to mirror wrangler's Text rule.
export default defineConfig({});
